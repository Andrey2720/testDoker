pipeline {
    agent any
    stages {
        stage('ClonRep') {
            steps {
                sh 'ls -la'
                sh 'sudo ./pomoika docker compose up --build'
            }
        }
       
    }
}

