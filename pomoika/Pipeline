pipeline {
    agent any
    stages {
        stage('ClonRep') {
            steps {
                sh 'ls -la'
                sh 'cd pomoika'
                sh 'docker compose up --build'
            }
        }
       
    }
}