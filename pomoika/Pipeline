pipeline {
    agent any
    stages {
        stage('ClonRep') {
            steps {
                sh 'ls -la'
                sh './pomoika docker compose up --build'
            }
        }
       
    }
}