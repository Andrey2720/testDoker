pipeline {
    agent any
    stages {
        stage('ClonRep') {
            steps {
                sh 'ls -la'
                sh 'whoami'
                sh 'cd pomoika || docker compose up --build'
            }
        }
       
    }
}