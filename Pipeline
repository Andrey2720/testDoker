pipeline {
    agent any
    stages {
        stage('ClonRep') {
            steps {
                sh 'git clone https://github.com/Andrey2720/testDoker.git'
            }
        }
        stage('StartDocker') {
            steps {
                sh 'cd /home/admin/testDoker'
                sh 'ls -la'
            }
        }
    }
}