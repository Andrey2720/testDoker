pipeline {
    agent any
    stages {
        stage('ClonRep') {
            steps {
                sh 'ls -la'
                sh 'sudo docker compose up --build'
            }
        }
       
    }
}